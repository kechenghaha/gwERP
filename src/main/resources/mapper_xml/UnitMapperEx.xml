<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gw.erp.datasource.mappers.UnitMapperEx">
    <select id="selectByConditionUnit" parameterType="com.gw.erp.datasource.entities.UnitExample" resultMap="com.gw.erp.datasource.mappers.UnitMapper.BaseResultMap">
        select *
        FROM gw_unit
        where 1=1
        <if test="name != null">
            and name like '%${name}%'
        </if>
        and ifnull(delete_flag,'0') !='1'
        <if test="offset != null and rows != null">
            limit #{offset},#{rows}
        </if>
    </select>
    <select id="countsByUnit" resultType="java.lang.Long">
        SELECT
        COUNT(id)
        FROM gw_unit
        WHERE 1=1
        <if test="name != null">
            and name like '%${name}%'
        </if>
        and ifnull(delete_flag,'0') !='1'
    </select>
    <update id="batchDeleteUnitByIds">
        update gw_unit
        set delete_flag='1'
        where 1=1
        and id in (
        <foreach collection="ids" item="id" separator=",">
            #{id}
        </foreach>
        )
    </update>
</mapper>